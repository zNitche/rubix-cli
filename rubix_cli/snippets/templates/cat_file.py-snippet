import uos

<mod>file_exists</mod>

path = "<var>path</var>"

if not file_exists(path):
    print(f"'{path}'' doesn't exist")
    return

with open(path, "rb") as file:
    for line in file.readline():
        print(line.decode())
