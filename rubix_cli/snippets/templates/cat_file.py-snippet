import uos

<mod>file_exists</mod>

path = "<var>path</var>"

if not file_exists(path):
    print(f"'{path}'' doesn't exist")

else:
    with open(path, "r") as file:
        for line in file.readlines():
            print(line, end="")
