import uos


path = "<var>path</var>"

dirname = "<var>dirname</var>"
filename = "<var>filename</var>"

if dirname:
    uos.mkdir(path)
    print(f"created: {path}")

else:
    with open(path, "wb") as file:
        file.write(<var>file_content</var>)

    print(f"created: {path}")
