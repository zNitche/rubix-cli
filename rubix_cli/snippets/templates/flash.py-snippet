import uos


path = "<var>path</var>"

dirname = "<var>dirname</var>"
filename = "<var>filename</var>"
file_content = <var>file_content</var>

if dirname:
    uos.mkdir(path)
    print(f"created: {path}")

else:
    with open(path, "wb") as file:
        file.write(file_content)

    print(f"created: {path}")
