<com>parent snippet has to import uos module</com>


def file_exists(path: str) -> bool:
    if not path:
        return False

    try:
        uos.stat(path)

        return True
    except OSError:
        return False
